=====================================================
NEXTSTEP — WHAT'S LEFT TO DO BEFORE GOING LIVE
=====================================================
Generated for: Raj — NextStep Automation
Last updated: March 2026

This document lists every remaining task, what file
to edit, and exactly what to change. Work through
this list before selling to your first customer.

=====================================================
SECTION 1 — REPLACEMENTS (Quick fixes)
=====================================================

1. OWNER EMAIL
   Current value : raj.nextstepoutsourcing@gmail.com
   Replace with  : your business email e.g. raj@nextstep.co.uk
   Files to edit :
     - .env  →  OWNER_EMAIL=
     - .env  →  MAIL_USERNAME=
     - .env  →  MAIL_DEFAULT_SENDER=
     - app/auth/routes.py  →  line with owner_email fallback

2. OWNER PASSWORD
   Current value : Nextstep@2026
   Action        : Change this in .env → OWNER_PASSWORD=
   Important     : Do this BEFORE first deployment.
                   After first run it is hashed in the DB.
                   To change later: log in → Owner Settings → Change Password

3. GMAIL APP PASSWORD (for sending emails)
   Current value : placeholder — not set
   File to edit  : .env  →  MAIL_PASSWORD=your-16-char-app-password
   How to get it : myaccount.google.com/apppasswords
                   Name it NextStep, copy the 16-char code
   What it enables: forgot password emails + new registration alerts to you

4. DOMAIN URLS
   Current value : nextstep.co.uk (hardcoded in a few places)
   Files to edit :
     - app/auth/routes.py  →  _notify_owner_new_registration()
       Change: https://nextstep.co.uk/owner/dashboard
       To    : your real domain
     - All HTML templates  →  footer copyright links
     - render.yaml         →  update service name if needed

5. SECRET KEY
   Current value : auto-generated by Render (render.yaml: generateValue: true)
   Action        : No action needed — Render generates this automatically.
                   If running locally, set a long random string in .env → SECRET_KEY=

6. FORMSPREE FORM ID (landing page demo form)
   File          : index.html (frontend, not Flask)
   Current value : https://formspree.io/raj.nextstepoutsourcing@gmail.com
   Action        : Go to formspree.io → sign up → create form → get ID
                   Replace with: https://formspree.io/f/YOUR_ACTUAL_ID
   What it does  : Routes demo request form submissions to your email

=====================================================
SECTION 2 — BACKEND CONNECTIONS (Most important)
=====================================================

7. CONNECT NMC TOOL TO CENTRAL AUTH
   What to do    : NMC tool must check the JWT/session token from the
                   main NextStep app before allowing any job submission.
   Files to edit : Your NMC app's route handlers
   How           : On every job submission:
                   - Receive tenant_id, user_id from the session
                   - Create a Job record in the central DB
                   - Store outputs under storage/tenant_id/user_id/job_id/
                   - Write back job status to the central jobs table

8. CONNECT DBS TOOL TO CENTRAL AUTH
   Same as above but for your DBS service.
   Files to edit : Your DBS app → app.py route handlers

9. CONNECT RTW TOOL TO CENTRAL AUTH
   Same pattern.
   Files to edit : Your RTW app route handlers

10. CONNECT CHECKLIST TOOL TO CENTRAL AUTH
    Same pattern.

11. SEED TOOLS TABLE
    The tools table needs 4 rows inserted for NMC, DBS, RTW, Checklist.
    Add this to app/__init__.py inside _seed_owner() after owner is created:
    
    from app.models import Tool
    tools_data = [
        {'slug': 'nmc',       'display_name': 'NMC Check',      'token_cost': 1},
        {'slug': 'dbs',       'display_name': 'DBS Check',      'token_cost': 1},
        {'slug': 'rtw',       'display_name': 'Right to Work',  'token_cost': 1},
        {'slug': 'checklist', 'display_name': 'Checklist',      'token_cost': 1},
    ]
    for t in tools_data:
        if not Tool.query.filter_by(slug=t['slug']).first():
            db.session.add(Tool(**t))
    db.session.commit()
    
    Note: token_cost per tool — set to 1 for now, update when pricing confirmed.

=====================================================
SECTION 3 — FEATURES TO BUILD NEXT
=====================================================

12. TENANT USER DASHBOARD — TEAM MANAGEMENT
    Current state : Tenant dashboard shows tools but Team link goes nowhere (#)
    File to edit  : app/dashboard/routes.py — add /dashboard/team route
    Template      : Create app/templates/dashboard/tenant_team.html
    What it needs : List team members, invite new staff, change roles

13. TENANT USER DASHBOARD — USAGE PAGE
    Current state : Usage link in tenant sidebar goes nowhere (#)
    File to edit  : app/dashboard/routes.py — add /dashboard/usage route
    Template      : Create app/templates/dashboard/tenant_usage.html
    What it shows : Token usage this month, jobs run, tool breakdown

14. TENANT USER DASHBOARD — BILLING PAGE
    Current state : Billing link goes nowhere (#)
    File to edit  : app/dashboard/routes.py — add /dashboard/billing route
    Template      : Create app/templates/dashboard/tenant_billing.html
    What it shows : Current plan, tokens remaining, renewal date, payment method

15. TOOL LINKS IN TENANT DASHBOARD
    Current state : NMC/DBS/RTW/Checklist cards link to # (placeholder)
    File to edit  : app/templates/dashboard/tenant_dashboard.html
    Replace href="#" with your actual tool subdomains:
      NMC       → https://nmc.nextstep.co.uk
      DBS       → https://dbs.nextstep.co.uk
      RTW       → https://rtw.nextstep.co.uk
      Checklist → https://checklist.nextstep.co.uk

16. JOB HISTORY — DOWNLOAD LINKS
    Current state : Jobs table shows status but no download button
    What to add   : A download column that links to the output file
                    protected by ownership check before serving the file

=====================================================
SECTION 4 — SECURITY HARDENING BEFORE LAUNCH
=====================================================

17. RATE LIMITING ON LOGIN
    What          : Prevent brute force attacks on /login
    How           : pip install Flask-Limiter
                    Add @limiter.limit("10 per minute") to auth/routes.py login route

18. HTTPS ONLY
    What          : Render handles HTTPS automatically on paid tier
    Action        : Make sure SESSION_COOKIE_SECURE=True in production
                    Already set in app/__init__.py when FLASK_ENV=production

19. DOWNLOAD AUTHORIZATION
    What          : Any route that serves output files must verify
                    that the requesting user owns that job
    How           : In every file download route:
                    job = Job.query.get(job_id)
                    if job.tenant_id != current_user.tenant_id: abort(403)

20. CSRF PROTECTION
    What          : Flask-WTF CSRF is already in requirements.txt
    Action        : Add CSRFProtect(app) to app/__init__.py
                    Add <input type="hidden" name="csrf_token" value="{{ csrf_token() }}"/>
                    to every POST form in templates

=====================================================
SECTION 5 — BEFORE FIRST PAYING CUSTOMER
=====================================================

21. TEST THE FULL FLOW END TO END
    - Register a test company from the register page
    - Log in as owner, see the pending registration
    - Activate the company + grant tokens
    - Log in as the test company user
    - Verify they see the dashboard with correct token count
    - Run a test job on NMC/DBS (when connected)
    - Check job appears in owner dashboard

22. SET UP POSTGRESQL ON RENDER
    - Create a PostgreSQL database on Render (free tier available)
    - Copy the Internal Connection String
    - Paste into render.yaml or Render environment variables as DATABASE_URL

23. COPY FRONTEND HTML FILES INTO FLASK TEMPLATES OR STATIC
    The 6 frontend HTML files (index, login, register, about, privacy, terms)
    need to be served by Flask.
    Options:
      A) Move index.html, about.html, privacy-policy.html, terms.html
         into app/templates/ and create simple Flask routes for them
      B) Serve them as static files from a /static folder
    Recommended: Option A — add these routes to a new app/main/routes.py:
      @main_bp.route('/')       → render_template('index.html')
      @main_bp.route('/about')  → render_template('about.html')
      @main_bp.route('/privacy-policy') → render_template('privacy-policy.html')
      @main_bp.route('/terms')  → render_template('terms.html')

=====================================================
SECTION 6 — WHEN DOMAIN IS PURCHASED
=====================================================

24. UPDATE ALL EMAIL REFERENCES
    Files:
      - .env → OWNER_EMAIL, MAIL_USERNAME, MAIL_DEFAULT_SENDER
      - app/auth/routes.py → _notify_owner_new_registration fallback
      - All HTML templates → footer contact/privacy links

25. UPDATE RENDER SERVICE URLs
    In render.yaml update the service name and any hardcoded domain references.

26. POINT DOMAIN DNS TO RENDER
    In your domain registrar (Namecheap, GoDaddy etc):
      - Add CNAME record: @ → your-render-service.onrender.com
      - Add CNAME record: www → your-render-service.onrender.com
      - For subdomains: nmc → nmc-service.onrender.com (etc)

27. REPLACE FORMSPREE EMAIL IN INDEX.HTML
    File   : index.html (or app/templates/index.html after integration)
    Change : action="https://formspree.io/raj.nextstepoutsourcing@gmail.com"
    To     : action="https://formspree.io/f/YOUR_FORMSPREE_ID"
             (get this from formspree.io after signing up)

=====================================================
SECTION 7 — NICE TO HAVE (After first customers)
=====================================================

28. STRIPE BILLING INTEGRATION
    What  : Automate subscription payments
    How   : pip install stripe
            Add Stripe webhook handler
            Connect to Subscription model

29. QUEUE + WORKER MODEL FOR TOOLS
    What  : Use Redis + RQ or Celery for heavy Playwright jobs
    Why   : Prevents crashes under load on Render

30. AUTOMATED TOKEN TOP-UP WHEN SUBSCRIPTION RENEWS
    What  : Reset tenant.tokens_used = 0 and tokens_total = plan amount
            on subscription renewal date

31. INVITE TEAM MEMBERS BY EMAIL
    What  : Owner of a company can invite staff via email link
    Where : Tenant dashboard → Team page

=====================================================
SUMMARY — DO THIS IN ORDER
=====================================================

BEFORE DEPLOYING:
  1. Set OWNER_PASSWORD in .env
  2. Set SECRET_KEY in .env (or let Render generate)
  3. Set DATABASE_URL in .env (from Render PostgreSQL)
  4. Add tool seeding to _seed_owner() (Section 2, item 11)
  5. Integrate frontend HTML files into Flask (Section 5, item 23)

AFTER DEPLOYING:
  6. Test registration → activation → login flow
  7. Set MAIL_PASSWORD (Gmail App Password) for email sending
  8. Replace Formspree ID in index.html

WHEN DOMAIN IS READY:
  9. Update all email references
 10. Update DNS records
 11. Replace all nextstep.co.uk placeholders

BEFORE FIRST CUSTOMER:
 12. Connect NMC/DBS/RTW/Checklist to central auth (Section 2)
 13. Add rate limiting to login (Section 4, item 17)
 14. Add CSRF protection (Section 4, item 20)
 15. Test end-to-end full flow (Section 5, item 21)

=====================================================
END OF DOCUMENT
=====================================================
